---
phase: bugfix/general-refactoring (ad-hoc, no GSD phase)
task: complete
status: done — build clean
last_updated: 2026-05-07T08:42:58Z
---

<current_state>
Refactoring "zero loading times" completato e buildato senza errori.
Branch: bugfix/general-refactoring
Tutto il codice è unstaged — da committare.
</current_state>

<completed_work>

- SQLite migration v5: tabella `watch_providers` (TTL 24h) + colonna `vote_count` su `detail_cache`
- 4 protocolli: WatchProvidersRepositoryProtocol, MediaDetailRepositoryProtocol, DiscoveryRepositoryProtocol, ListRepositoryProtocol
- 8 repository in `Core/Repositories/` (Local + Live per ciascun dominio)
- LiveWatchProvidersRepository: unico punto di rete per i providers, merge TMDB+RapidAPI centralizzata, TTL 24h SQLite
- MovieDetailViewModel + TVShowDetailViewModel: PRO-gate rimosso, injettati mediaDetailRepository + providersRepository, merge logic rimossa (ora nel repo)
- DiscoveryViewModel: usa LiveDiscoveryRepository via protocollo
- ListsView.loadProviders(): usa LiveWatchProvidersRepository (niente più rete ad ogni card appear)
- DetailCacheService: voteCount persistito e letto correttamente
- DependencyContainer: aggiornato per i nuovi init VM
- AppRepositories singleton
</completed_work>

<remaining_work>

- Committare le modifiche (git commit)
- Test manuali: fresh install + secondo lancio per verificare zero-loading su tutte e 4 le viste
- Test con Proxyman/Charles: verificare che nelle liste la sola chiamata di rete sia verso providers, e solo se TTL scaduto
- Opzionale: aggiungere unit test per Local* repos (TTL, read/write happy path)
- Bug list-fetching noti (fetchListItems senza deleted_at IS NULL, applyLists overwrite) — separati, non in scope
</remaining_work>

<decisions_made>

- TTL providers = 24h (concordato con utente)
- Architettura: Local/Live repository protocol pairs (concordato)
- PRO-gate rimosso: tutti gli utenti scrivono in detail_cache (concordato)
- Bug list-fetching fuori scope (concordato)
- LiveMediaDetailRepository non chiama getMovieExternalIds separatamente: movie.imdbId viene da /movie/{id} direttamente
- detail_cache.providers_json diventa legacy: source of truth per providers è watch_providers table
</decisions_made>

<blockers>
Nessuno. Build clean.
</blockers>

<context>
Refactoring architetturale completato. L'architettura ora ha:
- Una sola sorgente di rete per i providers (LiveWatchProvidersRepository) con TTL 24h
- Cache-first su tutte le 4 viste (Discovery già era OK, Lists/Detail ora migliorati)
- PRO-gate rimosso: cache scritta per tutti gli utenti
- Protocol-based DI per testabilità futura

Il build è pulito. Serve solo commit + testing manuale.
</context>

<next_action>
Committare: `git add` i file modificati/nuovi, `git commit -m "feat: zero-loading cache-first refactor for Discovery/Lists/Detail views"`

File da aggiungere:
- VibeWatchApp/Core/Repositories/ (nuova cartella intera)
- VibeWatchApp/Core/Database/SQLiteService.swift
- VibeWatchApp/Core/Services/DetailCacheService.swift
- VibeWatchApp/Features/Discovery/ViewModels/DiscoveryViewModel.swift
- VibeWatchApp/Features/Discovery/ViewModels/MovieDetailViewModel.swift
- VibeWatchApp/Features/Lists/Views/ListsView.swift
- VibeWatchApp/App/DI/DependencyContainer.swift
</next_action>

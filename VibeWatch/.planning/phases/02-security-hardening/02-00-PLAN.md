---
phase: 02-security-hardening
plan: "00"
type: tdd
wave: 1
depends_on: []
files_modified:
  - VibeWatchAppTests/KeychainStorageTests.swift
  - VibeWatchAppTests/AuthMigrationTests.swift
autonomous: true
requirements:
  - SEC-02

must_haves:
  truths:
    - "KeychainStorageTests.swift exists in the test target and all tests fail with 'type not found' (RED baseline)"
    - "AuthMigrationTests.swift exists in the test target and all tests fail with 'type not found' (RED baseline)"
  artifacts:
    - path: "VibeWatchAppTests/KeychainStorageTests.swift"
      provides: "Failing unit test stubs covering KeychainStorage store/retrieve/remove/error"
      exports: ["KeychainStorageTests"]
    - path: "VibeWatchAppTests/AuthMigrationTests.swift"
      provides: "Failing unit test stubs covering migration success, failure (forced re-login), and idempotency"
      exports: ["AuthMigrationTests"]
  key_links:
    - from: "VibeWatchAppTests/KeychainStorageTests.swift"
      to: "VibeWatchApp/Core/Services/KeychainStorage.swift"
      via: "KeychainStorage type reference (does not exist yet — RED)"
      pattern: "KeychainStorage\\("
    - from: "VibeWatchAppTests/AuthMigrationTests.swift"
      to: "VibeWatchApp/Core/Services/AuthService.swift"
      via: "migrateUserDefaultsToKeychain() call (does not exist yet — RED)"
      pattern: "migrateUserDefaultsToKeychain"
---

<objective>
Wave 0: Write failing test stubs for the Keychain storage adapter and UserDefaults-to-Keychain migration that plan 02-02 will implement.

Purpose: Establish RED baseline per TDD protocol. Tests must compile but fail because the production types (KeychainStorage, migrateUserDefaultsToKeychain) do not exist yet.
Output: Two test files registered in the VibeWatchAppTests target, all tests failing with linker/type errors.
</objective>

<execution_context>
@/Users/nicola/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicola/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-security-hardening/02-CONTEXT.md
@.planning/phases/02-security-hardening/02-RESEARCH.md
@.planning/phases/02-security-hardening/02-VALIDATION.md

<interfaces>
<!-- Types that tests will reference — do not exist yet; tests must fail RED -->

KeychainStorage (to be created in plan 02-02):
```swift
// VibeWatchApp/Core/Services/KeychainStorage.swift
import Security

final class KeychainStorage: AuthLocalStorage {
    init(service: String = Bundle.main.bundleIdentifier ?? "com.vibewatch.auth")
    func store(key: String, value: Data) throws
    func retrieve(key: String) throws -> Data?
    func remove(key: String) throws
}

enum KeychainError: LocalizedError {
    case unhandledError(status: OSStatus)
}
```

AuthService migration method (to be added in plan 02-02):
```swift
// VibeWatchApp/Core/Services/AuthService.swift
// Called as first line of AuthService.init()
private func migrateUserDefaultsToKeychain()
```

Existing test class pattern (from TESTING.md):
```swift
final class ConflictResolverTests: XCTestCase {
    var resolver: ConflictResolver!
    override func setUp() { super.setUp(); resolver = ConflictResolver() }
    override func tearDown() { resolver = nil; super.tearDown() }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write KeychainStorageTests RED stubs</name>
  <files>VibeWatchAppTests/KeychainStorageTests.swift</files>
  <behavior>
    - Test 1 (store and retrieve): store Data under a key → retrieve returns that same Data
    - Test 2 (overwrite): store "v1", store "v2" under same key → retrieve returns "v2"
    - Test 3 (remove): store data, remove key → retrieve returns nil
    - Test 4 (remove missing key): remove a key that was never stored → no error thrown
    - Test 5 (retrieve missing key): retrieve a key that was never stored → returns nil (not an error)
    - Test 6 (accessibility): confirm kSecAttrAccessibleAfterFirstUnlock is used — verify by storing and reading a value; behavioral test only (no internal inspection of Keychain attributes)
    Use unique key names per test (UUID-suffixed) to avoid Keychain state leakage between tests.
    tearDown must call remove for all keys written in that test — clean Simulator Keychain.
  </behavior>
  <action>
    Create VibeWatchAppTests/KeychainStorageTests.swift referencing KeychainStorage (which does not exist yet — tests will fail to compile/link RED).

    File structure:
    - `import XCTest` — no @testable import needed if KeychainStorage is public; use `@testable import VibeWatchApp` to access internal types
    - Class: `final class KeychainStorageTests: XCTestCase`
    - MARK sections: Properties, Setup/Teardown, Tests
    - setUp: instantiate `KeychainStorage(service: "com.vibewatch.tests.keychain")`
    - tearDown: nil out sut
    - Each test: generate a unique key with `UUID().uuidString`, call sut methods, assert results, clean up via remove
    - Use `XCTAssertNoThrow`, `XCTAssertThrowsError`, `XCTAssertEqual`, `XCTAssertNil`

    Then register the file in the Xcode project: open VibeWatchApp.xcodeproj, add KeychainStorageTests.swift to the VibeWatchAppTests target. Use `sed` on the .pbxproj only if adding programmatically is needed — prefer Xcode file add, but since this is automated, add via pbxproj UUID insertion following the pattern established in phase 01 plan 01 (which registered test files programmatically).
  </action>
  <verify>
    <automated>MISSING — test file must be created first; after creation run:
xcodebuild build-for-testing -scheme VibeWatchApp -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | grep -E 'KeychainStorageTests|error:|BUILD'
Expected: compile error referencing KeychainStorage (RED baseline). If no error, KeychainStorage already exists — stop and alert.</automated>
  </verify>
  <done>KeychainStorageTests.swift exists in VibeWatchAppTests target, references KeychainStorage, and the build reports the type as undefined (RED). All 6 test method stubs are present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write AuthMigrationTests RED stubs</name>
  <files>VibeWatchAppTests/AuthMigrationTests.swift</files>
  <behavior>
    - Test 1 (success path): given UserDefaults populated with auth_cached_user and auth_cached_is_authenticated, after migration: (a) Keychain holds both values, (b) UserDefaults no longer has either key
    - Test 2 (failure path / re-login): given a mock KeychainStorage whose store() throws, after migration: (a) both UserDefaults keys are cleared, (b) Keychain has no data for either key, (c) AuthService session is nil (user is signed out)
    - Test 3 (idempotency): given UserDefaults keys already cleared (migration already ran), calling migration a second time is a no-op — no Keychain writes attempted, no error
    Tests isolate AuthService by constructing a fresh instance pointing at a test UserDefaults suite (suiteName: "com.vibewatch.tests.migration") and a test KeychainStorage service name.
    Use setUp/tearDown to clear both stores before and after each test.
  </behavior>
  <action>
    Create VibeWatchAppTests/AuthMigrationTests.swift referencing `AuthService.migrateUserDefaultsToKeychain()` (which does not exist yet — RED).

    File structure:
    - `@testable import VibeWatchApp`
    - Class: `final class AuthMigrationTests: XCTestCase`
    - Properties: `var testDefaults: UserDefaults!`, `var testKeychain: KeychainStorage!`
    - setUp: init both with isolated namespaces, clear them
    - tearDown: clear both, nil them out
    - Helper `private func populateUserDefaults()` sets `auth_cached_user` and `auth_cached_is_authenticated` keys
    - For the failure path test: define a `MockFailingKeychain: AuthLocalStorage` nested class inside the test class that always throws `KeychainError.unhandledError(status: errSecIO)` from `store()`
    - Each test calls `AuthService._migrateUserDefaultsToKeychain(from: testDefaults, to: testKeychain)` (internal/testable overload to accept injected stores) OR tests through AuthService.init() with injected defaults — leave as stub comment if the exact seam is unknown; the executor will choose the testable seam when implementing

    Register file in Xcode project pbxproj following same pattern as Task 1.
  </action>
  <verify>
    <automated>MISSING — file must be created first; after creation run:
xcodebuild build-for-testing -scheme VibeWatchApp -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | grep -E 'AuthMigrationTests|migrateUserDefaultsToKeychain|error:|BUILD'
Expected: compile error referencing missing type or method (RED baseline).</automated>
  </verify>
  <done>AuthMigrationTests.swift exists in VibeWatchAppTests target with 3 test method stubs. Build reports undefined reference to KeychainStorage or migrateUserDefaultsToKeychain (RED). MockFailingKeychain nested class is defined in the file.</done>
</task>

</tasks>

<verification>
Both test files exist in VibeWatchAppTests/ and are registered in the Xcode project target.
Build-for-testing fails with type/method not found errors (confirmed RED baseline).
No production code changes in this plan.
</verification>

<success_criteria>
- VibeWatchAppTests/KeychainStorageTests.swift: 6 test methods, builds to RED (KeychainStorage undefined)
- VibeWatchAppTests/AuthMigrationTests.swift: 3 test methods + MockFailingKeychain, builds to RED (type/method undefined)
- No changes to production Swift files
- Both files registered in VibeWatchApp.xcodeproj VibeWatchAppTests target
</success_criteria>

<output>
After completion, create `.planning/phases/02-security-hardening/02-00-SUMMARY.md`
</output>

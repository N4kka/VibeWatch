---
phase: 03-performance
plan: "00"
type: tdd
wave: 1
depends_on: []
files_modified:
  - VibeWatchAppTests/PerformanceTests.swift
autonomous: true
requirements:
  - PERF-01
  - PERF-02
  - PERF-03
  - PERF-04

must_haves:
  truths:
    - "PerformanceTests.swift exists in the VibeWatchAppTests target with all 6 test method stubs (RED baseline)"
    - "Build fails with 'type not found' or 'value of type has no member' errors because production types do not exist yet"
    - "No production code is modified in this plan"
  artifacts:
    - path: "VibeWatchAppTests/PerformanceTests.swift"
      provides: "Failing unit test stubs covering PERF-01 through PERF-04"
      exports: ["PerformanceTests"]
  key_links:
    - from: "VibeWatchAppTests/PerformanceTests.swift"
      to: "VibeWatchApp/App/VibeWatchApp.swift"
      via: "AppState.loadCachedContentSync() reference (does not exist as testable method yet — RED)"
      pattern: "loadCachedContentSync"
    - from: "VibeWatchAppTests/PerformanceTests.swift"
      to: "VibeWatchApp/Core/Database/SQLiteService.swift"
      via: "SQLiteService.readerQueue reference (does not exist yet — RED)"
      pattern: "readerQueue"
---

<objective>
Wave 0: Write failing test stubs for all four performance requirements that plans 03-01, 03-02, and 03-03 will make pass.

Purpose: Establish RED baseline per TDD protocol. Tests must reference types and methods that do not exist yet so the build fails, proving the baseline.
Output: One test file registered in the VibeWatchAppTests target. All 6 test stubs fail to compile because the production changes have not been made yet.
</objective>

<execution_context>
@/Users/nicola/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicola/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-performance/03-RESEARCH.md
@.planning/phases/03-performance/03-VALIDATION.md

<interfaces>
<!-- Types and methods tests reference — production changes do not exist yet; tests MUST fail RED -->

AppState (to be modified in plan 03-01):
```swift
// VibeWatchApp/App/VibeWatchApp.swift
// New stored property (does not exist yet):
private(set) var carouselsGeneratedThisLaunch: Bool  // Wave 0: does not exist — test will fail to compile
```

SQLiteService (to be modified in plan 03-02):
```swift
// VibeWatchApp/Core/Database/SQLiteService.swift
// New property (does not exist yet):
let readerQueue: DispatchQueue  // concurrent, .userInitiated — Wave 0: does not exist — test will fail
private(set) var hasPersonalizedDiscoveryCache: Bool  // set after createTables() — Wave 0: does not exist
```

SQLiteService.hasCachedPersonalizedContent() (to be added in plan 03-01):
```swift
// Synchronous check for personalized_discovery rows — does not exist yet
func hasCachedPersonalizedContent() -> Bool
```

Existing test class pattern (from phase 02):
```swift
final class KeychainStorageTests: XCTestCase {
    var sut: KeychainStorage!
    override func setUp() { super.setUp(); sut = KeychainStorage(service: "com.vibewatch.tests") }
    override func tearDown() { sut = nil; super.tearDown() }
}
```

Existing pbxproj file registration pattern: follow the approach used in phase 01 plan 01
(add file UUID + PBXBuildFile entry + PBXGroup entry + target membership to VibeWatchAppTests).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write PerformanceTests RED stubs</name>
  <files>VibeWatchAppTests/PerformanceTests.swift</files>
  <behavior>
    - testLoadCachedContentSyncReturnsTrueWithData (PERF-01): given SQLiteService with rows in personalized_discovery, SQLiteService.hasPersonalizedDiscoveryCache returns true
    - testLoadCachedContentSyncReturnsFalseWhenEmpty (PERF-01): given SQLiteService with no rows, hasPersonalizedDiscoveryCache returns false
    - testCarouselGeneratedOncePerLaunch (PERF-02): AppState.carouselsGeneratedThisLaunch starts false; after triggering carousel generation path it becomes true; a second call does not reset it to false
    - testConcurrentReadDoesNotBlockWrite (PERF-03): SQLiteService exposes a readerQueue DispatchQueue with .concurrent attributes; dispatching 10 concurrent read closures onto readerQueue does not dead-lock when a serial write runs on the writer queue simultaneously
    - testDiscoveryLoadsFromCacheBeforeNetwork (PERF-04): given DiscoveryPersonalizationService with a populated in-memory cache, generatePersonalizedCarousels returns immediately without calling the network (use a mock/stub that records network calls)
    - testClipsLoadsFromCacheBeforeNetwork (PERF-04): given ClipsRepository with a populated DatabaseClipsService returning rows, fetchClips() returns cached rows and does not invoke any network refresh before returning
  </behavior>
  <action>
    Create VibeWatchAppTests/PerformanceTests.swift.

    File structure:
    - `@testable import VibeWatchApp`
    - `import XCTest`
    - Class: `final class PerformanceTests: XCTestCase`
    - MARK sections: // MARK: - PERF-01 Tests, // MARK: - PERF-02 Tests, // MARK: - PERF-03 Tests, // MARK: - PERF-04 Tests
    - Each test method is a stub: call the production property/method that will be added, XCTAssert the expected result. At Wave 0, these references do not exist — the file MUST fail to compile RED.

    PERF-01 stubs: access `SQLiteService.shared.hasPersonalizedDiscoveryCache` (Bool). Reference this stored property which does not exist yet.

    PERF-02 stubs: access `AppState().carouselsGeneratedThisLaunch` (Bool). This property does not exist yet on AppState. Note: AppState may require init parameters — check existing usage in VibeWatchApp.swift to match the correct initialiser signature, or use a minimal stub call that will fail to compile because the property is absent.

    PERF-03 stubs: access `SQLiteService.shared.readerQueue` (DispatchQueue). This property does not exist yet. For the concurrency check, dispatch 10 concurrent async blocks onto readerQueue inside an XCTestExpectation loop, fulfil the expectation in each block, waitForExpectations(timeout: 5). Also verify the writer queue (dbQueue — but since dbQueue is private, stub the test as a comment noting "verify readerQueue.label == com.vibewatch.sqlite.reader" once the property exists).

    PERF-04 stubs: for Discovery, access DiscoveryPersonalizationService.shared.memoryCache (this property IS private — if not accessible via @testable, stub as XCTFail("memoryCache not accessible — implement cache-first test seam") so the test exists but immediately fails RED). For Clips, call ClipsRepository (check if it exists in the codebase — stub if not accessible from tests).

    All test bodies must either reference a nonexistent property/method (compile error RED) or call XCTFail("RED — implement production code in plans 03-01/03-02/03-03") as a fallback for cases where the property happens to exist. The goal is at least 4 of the 6 tests compile-RED due to missing members.

    Register PerformanceTests.swift in the Xcode project VibeWatchApp.xcodeproj pbxproj file:
    - Add a PBXFileReference entry for VibeWatchAppTests/PerformanceTests.swift
    - Add a PBXBuildFile entry
    - Add to the PBXGroup containing the other test files
    - Add to the VibeWatchAppTests target Sources build phase
    Follow the exact UUID format and pbxproj syntax used by the existing test file registrations (grep pbxproj for "KeychainStorageTests" to find the pattern).
  </action>
  <verify>
    <automated>xcodebuild build-for-testing -scheme VibeWatchApp -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | grep -E 'PerformanceTests|hasPersonalizedDiscoveryCache|readerQueue|carouselsGeneratedThisLaunch|error:|BUILD'</automated>
  </verify>
  <done>PerformanceTests.swift exists in VibeWatchAppTests/ and is registered in the Xcode project. Build-for-testing reports at least 3 compile errors referencing the missing production members (hasPersonalizedDiscoveryCache, readerQueue, carouselsGeneratedThisLaunch). All 6 test method stubs are present. No production files are modified.</done>
</task>

</tasks>

<verification>
PerformanceTests.swift is in VibeWatchAppTests/ and in the Xcode project target.
Build-for-testing fails with missing-member errors on properties introduced by plans 03-01/03-02.
No production code changes in this plan.
</verification>

<success_criteria>
- VibeWatchAppTests/PerformanceTests.swift: 6 test methods in 4 MARK sections, registered in VibeWatchAppTests target
- Build-for-testing reports compile errors on at least: hasPersonalizedDiscoveryCache, readerQueue, carouselsGeneratedThisLaunch (RED)
- No changes to any production Swift file
</success_criteria>

<output>
After completion, create `.planning/phases/03-performance/03-00-SUMMARY.md`
</output>

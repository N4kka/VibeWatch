import Foundation

// INSTRUCTIONS:
// Secrets are now provided via an untracked xcconfig file:
// 1. Copy `VibeWatchApp/Config/Secrets.xcconfig.template` to `Secrets.xcconfig`
// 2. Fill in your keys
// 3. The app reads them from Info.plist at runtime
